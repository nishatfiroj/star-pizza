import styled from "styled-components"

export const CallButton = styled.a`
  background-color: var(--secondary-color);
  border-radius: 24px;
  padding: 12px 24px;
  text-align: center;
  color: var(--default-white);
  font-weight: bold;
  max-width: 400px;
`

export const OrderOnlineButton = styled.a`
  background-color: var(--default-text-color);
  color: var(--default-white);
  border-radius: 24px;
  padding: 12px 24px;
  text-align: center;
  color: var(--default-white);
  font-weight: bold;
  max-width: 400px;
`
